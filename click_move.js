/*
import React, { useState } from "react";
import ReactDOM from "react-dom";
import "./clickmove.css";

const Hello = () => {
    return <h2 > hello < /h2>;
};

const Bye = () => {
    return <div > < button > button2 < /button>  <
    h2 > bye < /h2 > < div > ;
};

const App1 = () => {
    const [toggled, setToggled] = useState(true);
    const handleClick = () => {
        setToggled(!toggled);
    };

    const render = () => {
        if (toggled) {
            return <Hello / > ;
        }

        return <Bye / > ;
    };

    return ( <
        div >
        <
        button onClick = { handleClick } > toggle < /button> { render() } < /
        div >
    );
};
export default App1;
//ReactDOM.render( < App / > , document.getElementById("root"));
*/